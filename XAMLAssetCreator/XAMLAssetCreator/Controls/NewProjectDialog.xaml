<ContentDialog
    x:Class="XAMLAssetCreator.Controls.NewProjectDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:XAMLAssetCreator.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Create Project"
    CloseButtonClick="OnCancel"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    PrimaryButtonClick="CreateProject"
    PrimaryButtonText="Create Project"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock
            Margin="8"
            VerticalAlignment="Center"
            Text="Project Name" />
        <TextBox
            x:Name="ProjectNameBox"
            Grid.Column="1"
            MinWidth="200"
            Margin="8"
            VerticalAlignment="Center" />
    </Grid>
</ContentDialog>
